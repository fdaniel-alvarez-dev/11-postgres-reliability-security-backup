name: tests
on:
  push:
  pull_request:

jobs:
  demo-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run demo-mode tests (no external dependencies)
        run: |
          make test
